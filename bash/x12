while true
do
  read FOLDER
  if [ -d $FOLDER ]
  then
    OLDFOLDER="$PWD/$FOLDER"
    NEWFOLDER="$OLDFOLDER"b
    echo "duplicate as $NEWFOLDER"

    declare -a files=($(ls $OLDFOLDER))
    for f in  ${files[@]}
    do
      echo $f
    done

  else
    break
  fi
done
