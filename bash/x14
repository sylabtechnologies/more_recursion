Absolute()
{
    if (( $1 < 0))
    then
        return $(( - $1 ))
    else
        return $1
    fi
}

sum=0
for f in $@
do
    echo $f
    Absolute $f
    ret=$?
    sum=$(( $sum + $ret))
done

echo "absolute sum : $sum"

