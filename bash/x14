Absolute()
{
    if (( $1 < 0))
    then
        return $(( - $1 ))
    else
        return $1
    fi
}

echo "absolute sum :"

sum=0

for f in $@
do
    echo $f
    Absolute $f
    sum= $(( $sum + $?))
done

echo $sum

